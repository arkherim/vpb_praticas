services:
  - type: web
    name: vpb-praticas
    env: python
    runtime: python-3.11
    rootDir: .
    buildCommand: pip install -r app/backend/requirements.txt
    startCommand: python -m app.serve
    autoDeploy: true
